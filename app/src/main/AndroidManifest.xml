<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="ParyavaranKavalu"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ParyavaranKavalu">

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBqLjcumUBqKz5Bkzm_d0MPTQt33gm2XeM"/>

        <!-- Splash Screen -->
        <activity
            android:name=".SplashActivity"
            android:exported="true">

            <intent-filter>

                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />

            </intent-filter>

        </activity>

        <!-- Login -->
        <activity
            android:name=".LoginActivity"
            android:exported="false"/>

        <!-- Register -->
        <activity android:name=".RegisterActivity"/>

        <!-- Home -->
        <activity android:name=".HomeActivity"/>

        <!-- Report Waste -->
        <activity android:name=".ReportWasteActivity"/>

        <!-- View Reports -->
        <activity android:name=".ViewReportsActivity"/>

        <!-- Maps -->
        <activity android:name=".MapsActivity"/>

        <!-- Admin Login -->
        <activity android:name=".AdminLoginActivity"/>

        <!-- Admin Dashboard -->
        <activity android:name=".AdminDashboardActivity"/>

    </application>

</manifest>
